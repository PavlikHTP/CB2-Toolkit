<UserControl x:Class="CB2Toolkit.UIEditor.Views.UIEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CB2Toolkit.UIEditor"
             xmlns:views="clr-namespace:CB2Toolkit.UIEditor.Views"
             mc:Ignorable="d"
             d:DesignHeight="750" d:DesignWidth="1200"
             PreviewKeyDown="View_PreviewKeyDown"
             Language="en-US">
    <UserControl.Resources>
        <views:NormalizedToAbsoluteConverter x:Key="NormConverter" />
        <views:RgbToBrushConverter x:Key="RgbConverter" />

        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#232329" />
            <Setter Property="Foreground" Value="#F3F4F6" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2D2D35" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.4" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ToolboxButtonStyle" TargetType="Button" BasedOn="{StaticResource ToolbarButtonStyle}">
            <Setter Property="Background" Value="#1A1A1E" />
            <Setter Property="BorderBrush" Value="#2D2D35" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="0,0,0,8" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="0,10" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#232329" />
            <Setter Property="Foreground" Value="#E2E2E9" />
            <Setter Property="BorderBrush" Value="#3F3F46" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="CaretBrush" Value="#4D7CFE" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#52525B" />
                            </Trigger>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4D7CFE" />
                                <Setter Property="Background" Value="#16161A" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionIconButtonStyle" TargetType="Button" BasedOn="{StaticResource ToolbarButtonStyle}">
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Background" Value="Transparent" />
        </Style>

        <Style x:Key="ActionToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#9CA3AF" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#232329" />
                    <Setter Property="Foreground" Value="#F3F4F6" />
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#1A1A22" />
                    <Setter Property="Foreground" Value="#4D7CFE" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="#121214" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="{TemplateBinding Background}">
                            <Track Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#3F3F46" CornerRadius="4" Margin="2" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter Grid.Column="0" Grid.Row="0" Margin="{TemplateBinding Padding}" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" CanContentScroll="{TemplateBinding CanContentScroll}" />
                            <ScrollBar Name="PART_VerticalScrollBar" Grid.Column="1" Grid.Row="0" Value="{TemplateBinding VerticalOffset}" Maximum="{TemplateBinding ScrollableHeight}" ViewportSize="{TemplateBinding ViewportHeight}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />
                            <ScrollBar Name="PART_HorizontalScrollBar" Grid.Column="0" Grid.Row="1" Orientation="Horizontal" Value="{TemplateBinding HorizontalOffset}" Maximum="{TemplateBinding ScrollableWidth}" ViewportSize="{TemplateBinding ViewportWidth}" Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" />
                            <Rectangle Grid.Column="1" Grid.Row="1" Fill="#121214" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#D4D4D4" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}" Margin="0,0,0,4">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1C1C22" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#232329" />
                    <Setter Property="Foreground" Value="#FFFFFF" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#121214">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1A1A1E" Padding="16,6" BorderBrush="#232329" BorderThickness="0,0,0,1">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button Content="⬅ Back to Hub" Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,12,0" Click="BackToMenu_Click" Cursor="Hand" />
                    <Button Content="📁 Select Folder" Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,12,0" Click="SelectFolder_Click" Cursor="Hand" />
                    <Button Content="📥 Import .as" Style="{StaticResource ToolbarButtonStyle}" Click="ImportUI_Click" Cursor="Hand" Background="#232329" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Compile Path:" Foreground="#6B7280" VerticalAlignment="Center" Margin="0,0,8,0" FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold" />
                    <TextBox x:Name="CompilePathInput" Width="220" VerticalAlignment="Center" Margin="0,0,16,0" Style="{StaticResource ModernTextBoxStyle}" IsReadOnly="True" Background="#16161A" />

                    <TextBlock Text="Class Name:" Foreground="#6B7280" VerticalAlignment="Center" Margin="0,0,8,0" FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold" />
                    <TextBox x:Name="OutputNameInput" Text="MyUI" Width="110" VerticalAlignment="Center" Margin="0,0,16,0" Style="{StaticResource ModernTextBoxStyle}" />

                    <Button Content="✨ Generate UI" Background="#4D7CFE" Foreground="White" Padding="16,8" Style="{StaticResource ToolbarButtonStyle}" Click="GenerateUI_Click" Cursor="Hand" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="10" />
                <RowDefinition Height="200" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="65" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="260" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#16161A" CornerRadius="6,6,0,0" BorderBrush="#232329" BorderThickness="1,1,1,0" Padding="0,8">
                        <TextBlock Text="TOOLS" HorizontalAlignment="Center" Foreground="#4B5563" FontFamily="Segoe UI" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" />
                    </Border>
                    <Border Grid.Row="1" Background="#16161A" CornerRadius="0,0,6,6" BorderBrush="#232329" BorderThickness="1">
                        <ScrollViewer Padding="8">
                            <StackPanel>
                                <Button Content="🟦" ToolTip="Add Rect" Style="{StaticResource ToolboxButtonStyle}" Click="AddRect_Click" />
                                <Button Content="⭕" ToolTip="Add Oval" Style="{StaticResource ToolboxButtonStyle}" Click="AddOval_Click" />
                                <Button Content="📝" ToolTip="Add Text" Style="{StaticResource ToolboxButtonStyle}" Click="AddText_Click" />
                                <Button Content="🖼" ToolTip="Add Image" Style="{StaticResource ToolboxButtonStyle}" Click="AddImage_Click" />
                                <Button Content="📊" ToolTip="Add Progress Bar" Style="{StaticResource ToolboxButtonStyle}" Click="AddProgressBar_Click" />
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>

                <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="Transparent" Cursor="SizeWE" />

                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#16161A" CornerRadius="6,6,0,0" BorderBrush="#232329" BorderThickness="1,1,1,0" Padding="10,8">
                        <DockPanel>
                            <TextBlock Text="HIERARCHY" Foreground="#4B5563" FontFamily="Segoe UI" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" />
                            <Button Content="🗑" HorizontalAlignment="Right" Style="{StaticResource ActionIconButtonStyle}" Click="DeleteElement_Click" ToolTip="Delete Selected" />
                        </DockPanel>
                    </Border>
                    <Border Grid.Row="1" Background="#16161A" CornerRadius="0,0,6,6" BorderBrush="#232329" BorderThickness="1">
                        <ListBox x:Name="ElementsList" Background="Transparent" BorderThickness="0" Padding="8" FontFamily="Segoe UI" FontSize="13" SelectionChanged="ElementsList_SelectionChanged">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Icon}" Margin="0,0,8,0" />
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>
                </Grid>

                <GridSplitter Grid.Column="3" HorizontalAlignment="Stretch" Background="Transparent" Cursor="SizeWE" />

                <Grid Grid.Column="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#16161A" CornerRadius="6,6,0,0" BorderBrush="#232329" BorderThickness="1,1,1,0" Padding="14,8">
                        <TextBlock Text="VISUAL PREVIEW (DRAG &amp; DROP)" Foreground="#4B5563" FontFamily="Segoe UI" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" />
                    </Border>
                    <Border Grid.Row="1" Background="#0A0A0C" CornerRadius="0,0,6,6" BorderBrush="#232329" BorderThickness="1" ClipToBounds="True">
                        <ItemsControl x:Name="VisualPreviewContainer">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas x:Name="PreviewWorkspace" Background="Transparent" IsItemsHost="True" MouseLeftButtonDown="PreviewWorkspace_MouseLeftButtonDown" MouseLeftButtonUp="PreviewWorkspace_MouseLeftButtonUp" MouseMove="PreviewWorkspace_MouseMove" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Canvas.Left">
                                        <Setter.Value>
                                            <MultiBinding Converter="{StaticResource NormConverter}">
                                                <Binding Path="X" />
                                                <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Canvas}" />
                                            </MultiBinding>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Canvas.Top">
                                        <Setter.Value>
                                            <MultiBinding Converter="{StaticResource NormConverter}">
                                                <Binding Path="Y" />
                                                <Binding Path="ActualHeight" RelativeSource="{RelativeSource AncestorType=Canvas}" />
                                            </MultiBinding>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Background="Transparent" Cursor="Hand">
                                        <Grid.Style>
                                            <Style TargetType="Grid">
                                                <Setter Property="Width">
                                                    <Setter.Value>
                                                        <MultiBinding Converter="{StaticResource NormConverter}">
                                                            <Binding Path="Width" />
                                                            <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Canvas}" />
                                                        </MultiBinding>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="Height">
                                                    <Setter.Value>
                                                        <MultiBinding Converter="{StaticResource NormConverter}">
                                                            <Binding Path="Height" />
                                                            <Binding Path="ActualHeight" RelativeSource="{RelativeSource AncestorType=Canvas}" />
                                                        </MultiBinding>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Type}" Value="Text">
                                                        <Setter Property="Width" Value="Auto" />
                                                        <Setter Property="Height" Value="Auto" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>

                                        <Border x:Name="BoundingBox" BorderBrush="#4D7CFE" BorderThickness="1" Visibility="Collapsed" SnapsToDevicePixels="True" />

                                        <Border x:Name="RectVisual" CornerRadius="0" Visibility="Collapsed">
                                            <Border.Background>
                                                <MultiBinding Converter="{StaticResource RgbConverter}">
                                                    <Binding Path="R" />
                                                    <Binding Path="G" />
                                                    <Binding Path="B" />
                                                    <Binding Path="Opacity" />
                                                </MultiBinding>
                                            </Border.Background>
                                        </Border>

                                        <Ellipse x:Name="OvalVisual" Visibility="Collapsed">
                                            <Ellipse.Fill>
                                                <MultiBinding Converter="{StaticResource RgbConverter}">
                                                    <Binding Path="R" />
                                                    <Binding Path="G" />
                                                    <Binding Path="B" />
                                                    <Binding Path="Opacity" />
                                                </MultiBinding>
                                            </Ellipse.Fill>
                                        </Ellipse>

                                        <TextBlock x:Name="TextVisual" Text="{Binding Text}" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
                                            <TextBlock.Foreground>
                                                <MultiBinding Converter="{StaticResource RgbConverter}">
                                                    <Binding Path="R" />
                                                    <Binding Path="G" />
                                                    <Binding Path="B" />
                                                    <Binding Path="Opacity" />
                                                </MultiBinding>
                                            </TextBlock.Foreground>
                                        </TextBlock>

                                        <Grid x:Name="ImageVisual" Visibility="Collapsed">
                                            <Border BorderBrush="#4D7CFE" BorderThickness="1" Background="#1F2937" Opacity="0.6" />
                                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <TextBlock Text="🖼" HorizontalAlignment="Center" FontSize="16" />
                                                <TextBlock Text="{Binding Text}" Foreground="#9CA3AF" FontSize="10" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="4,0" />
                                            </StackPanel>
                                        </Grid>

                                        <Grid x:Name="ProgressVisual" Visibility="Collapsed">
                                            <Border Background="#1F2937" CornerRadius="3" BorderBrush="#374151" BorderThickness="1">
                                                <Grid>
                                                    <ProgressBar Value="70" Minimum="0" Maximum="100" Background="Transparent" BorderThickness="0">
                                                        <ProgressBar.Foreground>
                                                            <MultiBinding Converter="{StaticResource RgbConverter}">
                                                                <Binding Path="R" />
                                                                <Binding Path="G" />
                                                                <Binding Path="B" />
                                                                <Binding Path="Opacity" />
                                                            </MultiBinding>
                                                        </ProgressBar.Foreground>
                                                    </ProgressBar>
                                                    <TextBlock Text="Progress Bar" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" />
                                                </Grid>
                                            </Border>
                                        </Grid>

                                        <Thumb x:Name="ResizeGrip" Width="10" Height="10" HorizontalAlignment="Right" VerticalAlignment="Bottom" Cursor="SizeNWSE" DragDelta="ResizeThumb_DragDelta" Margin="0,0,-5,-5" Visibility="Collapsed">
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="Thumb">
                                                    <Border Background="#4D7CFE" CornerRadius="0" BorderBrush="White" BorderThickness="1"/>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </Grid>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding Type}" Value="Rect">
                                            <Setter TargetName="RectVisual" Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Type}" Value="Oval">
                                            <Setter TargetName="OvalVisual" Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Type}" Value="Text">
                                            <Setter TargetName="TextVisual" Property="Visibility" Value="Visible" />
                                            <Setter TargetName="ResizeGrip" Property="Opacity" Value="0" />
                                            <Setter TargetName="ResizeGrip" Property="IsEnabled" Value="False" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Type}" Value="Image">
                                            <Setter TargetName="ImageVisual" Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Type}" Value="ProgressBar">
                                            <Setter TargetName="ProgressVisual" Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                        
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="BoundingBox" Property="Visibility" Value="Visible" />
                                            <Setter TargetName="ResizeGrip" Property="Visibility" Value="Visible" />
                                        </Trigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                </Grid>

                <GridSplitter Grid.Column="5" HorizontalAlignment="Stretch" Background="Transparent" Cursor="SizeWE" />

                <Grid Grid.Column="6">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#1A1A1E" BorderBrush="#232329" BorderThickness="1,1,1,0" CornerRadius="6,6,0,0" Padding="14,8">
                        <TextBlock Text="PROPERTIES" Foreground="#E2E2E9" FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold" />
                    </Border>
                    <Border Grid.Row="1" Background="#16161A" BorderBrush="#232329" BorderThickness="1,0,1,1" CornerRadius="0,0,6,6" Padding="16">
                        <ScrollViewer>
                            <StackPanel x:Name="PropertiesPanel" Visibility="Collapsed">
                                <TextBlock Text="Element Name (Variable)" Foreground="#6B7280" FontSize="11" FontWeight="Bold" Margin="0,0,0,4" />
                                <TextBox Text="{Binding SelectedItem.Name, ElementName=ElementsList}" KeyDown="PropertyInput_KeyDown" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,14" />

                                <Grid Margin="0,0,0,14">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Position X" Foreground="#6B7280" FontSize="11" FontWeight="Bold" Margin="0,0,0,4" />
                                        <TextBox Text="{Binding SelectedItem.X, ElementName=ElementsList}" KeyDown="PropertyInput_KeyDown" Style="{StaticResource ModernTextBoxStyle}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Position Y" Foreground="#6B7280" FontSize="11" FontWeight="Bold" Margin="0,0,0,4" />
                                        <TextBox Text="{Binding SelectedItem.Y, ElementName=ElementsList}" KeyDown="PropertyInput_KeyDown" Style="{StaticResource ModernTextBoxStyle}" />
                                    </StackPanel>
                                </Grid>

                                <Grid Margin="0,0,0,14">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Width" Foreground="#6B7280" FontSize="11" FontWeight="Bold" Margin="0,0,0,4" />
                                        <TextBox Text="{Binding SelectedItem.Width, ElementName=ElementsList}" KeyDown="PropertyInput_KeyDown" Style="{StaticResource ModernTextBoxStyle}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Height" Foreground="#6B7280" FontSize="11" FontWeight="Bold" Margin="0,0,0,4" />
                                        <TextBox Text="{Binding SelectedItem.Height, ElementName=ElementsList}" KeyDown="PropertyInput_KeyDown" Style="{StaticResource ModernTextBoxStyle}" />
                                    </StackPanel>
                                </Grid>

                                <TextBlock Text="Text / Filename / Technique" Foreground="#6B7280" FontSize="11" FontWeight="Bold" Margin="0,0,0,4" />
                                <TextBox Text="{Binding SelectedItem.Text, ElementName=ElementsList}" KeyDown="PropertyInput_KeyDown" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,14" />

                                <Grid Margin="0,0,0,14">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Color R" Foreground="#6B7280" FontSize="11" FontWeight="Bold" Margin="0,0,0,4" />
                                        <TextBox Text="{Binding SelectedItem.R, ElementName=ElementsList}" KeyDown="PropertyInput_KeyDown" Style="{StaticResource ModernTextBoxStyle}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Color G" Foreground="#6B7280" FontSize="11" FontWeight="Bold" Margin="0,0,0,4" />
                                        <TextBox Text="{Binding SelectedItem.G, ElementName=ElementsList}" KeyDown="PropertyInput_KeyDown" Style="{StaticResource ModernTextBoxStyle}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="4">
                                        <TextBlock Text="Color B" Foreground="#6B7280" FontSize="11" FontWeight="Bold" Margin="0,0,0,4" />
                                        <TextBox Text="{Binding SelectedItem.B, ElementName=ElementsList}" KeyDown="PropertyInput_KeyDown" Style="{StaticResource ModernTextBoxStyle}" />
                                    </StackPanel>
                                </Grid>

                                <Grid Margin="0,0,0,14">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Opacity" Foreground="#6B7280" FontSize="11" FontWeight="Bold" Margin="0,0,0,4" />
                                        <TextBox Text="{Binding SelectedItem.Opacity, ElementName=ElementsList}" KeyDown="PropertyInput_KeyDown" Style="{StaticResource ModernTextBoxStyle}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Callback / Value" Foreground="#6B7280" FontSize="11" FontWeight="Bold" Margin="0,0,0,4" />
                                        <TextBox Text="{Binding SelectedItem.MiscValue, ElementName=ElementsList}" KeyDown="PropertyInput_KeyDown" Style="{StaticResource ModernTextBoxStyle}" />
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Grid>

            <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" Background="Transparent" Cursor="SizeNS" />

            <Border Grid.Row="2" Background="#0D0D10" CornerRadius="6" BorderBrush="#232329" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="32" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#131317" BorderBrush="#232329" BorderThickness="0,0,0,1" CornerRadius="5,5,0,0" Padding="12,0">
                        <DockPanel>
                            <TextBlock Text="CONSOLE OUTPUT" Foreground="#4B5563" FontFamily="Segoe UI" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" DockPanel.Dock="Left" />
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Button Content="📋 Copy All" Style="{StaticResource ActionIconButtonStyle}" Click="CopyConsole_Click" Cursor="Hand" Padding="8,4" Margin="0,0,6,0" />
                                <ToggleButton x:Name="AutoscrollToggle" Content="⬇ Autoscroll" IsChecked="True" Checked="AutoscrollToggle_Checked" Cursor="Hand" Margin="0,0,6,0" Style="{StaticResource ActionToggleButtonStyle}" />
                                <Button Content="🗑 Clear" Style="{StaticResource ActionIconButtonStyle}" Click="ClearConsole_Click" Cursor="Hand" Padding="8,4" />
                            </StackPanel>
                        </DockPanel>
                    </Border>

                    <ScrollViewer Name="ConsoleScrollViewer" Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl Name="ConsoleOutput" Padding="12">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Text, Mode=OneWay}" Foreground="{Binding Color}" FontFamily="Consolas" FontSize="13" TextWrapping="Wrap" Margin="0,0,0,2" IsReadOnly="True" Background="Transparent" BorderThickness="0" SelectionBrush="#3D4D7CFE" CaretBrush="Transparent" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <Border Grid.Row="2" Background="#111114" BorderBrush="#232329" BorderThickness="0,1,0,0" Padding="8,6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text=">" Foreground="#4D7CFE" FontFamily="Consolas" FontSize="13" FontWeight="Bold" VerticalAlignment="Center" Margin="6,0,8,0" />
                            <TextBox Grid.Column="1" x:Name="TerminalInput" Background="Transparent" Foreground="#D4D4D4" BorderThickness="0" FontFamily="Consolas" FontSize="13" VerticalAlignment="Center" KeyDown="TerminalInput_KeyDown" CaretBrush="#4D7CFE" />
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
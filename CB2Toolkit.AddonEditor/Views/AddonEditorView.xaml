<utils:LifecycleUserControl x:Class="CB2Toolkit.AddonEditor.Views.AddonEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
             xmlns:models1="clr-namespace:CB2Toolkit.Core.Models;assembly=CB2Toolkit.Core"
             xmlns:utils="clr-namespace:CB2Toolkit.CodeEditor.Utils;assembly=CB2Toolkit.CodeEditor"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100">

    <UserControl.Resources>
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#121214" />
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#E2E2E9" />
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#4D7CFE" />
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFFFFF" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#16161A" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#E2E2E9" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlLightBrushKey}" Color="#232329" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlDarkBrushKey}" Color="#121214" />

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1A1A1E" />
            <Setter Property="Foreground" Value="#E2E2E9" />
            <Setter Property="BorderBrush" Value="#4D7CFE" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="4,2" />
            <Setter Property="CaretBrush" Value="#4D7CFE" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E2E2E9" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border x:Name="Border" Width="16" Height="16" Background="#1A1A1E" BorderBrush="#4D7CFE" BorderThickness="1" CornerRadius="2">
                                <Path x:Name="Check" Data="M 2,5 L 5,8 L 11,2" Stroke="#4D7CFE" StrokeThickness="2" Visibility="Collapsed" Margin="2,1,0,0"/>
                            </Border>
                            <ContentPresenter Margin="6,0,0,0" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#232329" />
            <Setter Property="Foreground" Value="#F3F4F6" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2D2D35" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.4" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ActionIconButtonStyle" TargetType="Button" BasedOn="{StaticResource ToolbarButtonStyle}">
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Background" Value="Transparent" />
        </Style>

        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="#16161A" />
            <Setter Property="BorderBrush" Value="#232329" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="4">
                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="#E2E2E9" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Grid SnapsToDevicePixels="True">
                            <Border Name="Bd" Background="Transparent" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="CheckSymbol" />
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="Header" />
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="Arrow" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter ContentSource="Header" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" />
                                    <Popup Name="Popup" Placement="Right" IsOpen="{TemplateBinding IsSubmenuOpen}" Focusable="False" AllowsTransparency="True">
                                        <Border Background="#16161A" BorderBrush="#232329" BorderThickness="1" CornerRadius="6" Padding="4">
                                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                        </Border>
                                    </Popup>
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#232329" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="#D4D4D4" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Border Name="Bd" Background="Transparent" BorderThickness="0" Padding="4,6" SnapsToDevicePixels="true">
                                <Border.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="Create File" Click="ContextMenu_CreateFile_Click" />
                                        <MenuItem Header="Create Folder" Click="ContextMenu_CreateFolder_Click" />
                                        <Separator Background="#232329" Margin="0,4" />
                                        <MenuItem Header="Rename" Click="ContextMenu_Rename_Click" />
                                        <MenuItem Header="Delete" Click="ContextMenu_Delete_Click" />
                                        <Separator Background="#232329" Margin="0,4" />
                                        <MenuItem Header="Exclude from Index" Click="ContextMenu_Exclude_Click" />
                                        <Separator Background="#232329" Margin="0,4" />
                                        <MenuItem Header="Reveal in Explorer" Click="ContextMenu_RevealInExplorer_Click" />
                                    </ContextMenu>
                                </Border.ContextMenu>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="20" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <ToggleButton x:Name="Expander" ClickMode="Press" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" Focusable="False">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent" Width="16" Height="16">
                                                    <Path x:Name="Arrow" Data="M 6,4 L 10,8 L 6,12" Stroke="#6B7280" StrokeThickness="1.5" StrokeLineJoin="Round" StrokeEndLineCap="Round" StrokeStartLineCap="Round" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="Arrow" Property="Path.Data" Value="M 4,6 L 8,10 L 12,6" />
                                                        <Setter TargetName="Arrow" Property="Path.Stroke" Value="#9CA3AF" />
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Arrow" Property="Path.Stroke" Value="#F3F4F6" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <ContentPresenter Name="PART_Header" Grid.Column="1" ContentSource="Header" HorizontalAlignment="Stretch" VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                            <ItemsPresenter Name="ItemsHost" Grid.Row="1" Margin="14,0,0,0" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden" />
                            </Trigger>
                            <Trigger Property="IsExpanded" Value="false">
                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="Bd" Property="Background" Value="#232329" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" SourceName="Bd" Value="true">
                                <Setter TargetName="Bd" Property="Background" Value="#1C1C22" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="#121214" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="{TemplateBinding Background}">
                            <Track Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#3F3F46" CornerRadius="4" Margin="2" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Height" Value="14" />
                    <Setter Property="Width" Value="Auto" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid Background="{TemplateBinding Background}">
                                    <Track Name="PART_Track">
                                        <Track.Thumb>
                                            <Thumb>
                                                <Thumb.Template>
                                                    <ControlTemplate TargetType="Thumb">
                                                        <Border Background="#3F3F46" CornerRadius="4" Margin="2" />
                                                    </ControlTemplate>
                                                </Thumb.Template>
                                            </Thumb>
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="14" />
                    <Setter Property="Height" Value="Auto" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter Grid.Column="0" Grid.Row="0" Margin="{TemplateBinding Padding}" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" CanContentScroll="{TemplateBinding CanContentScroll}" />
                            <ScrollBar Name="PART_VerticalScrollBar" Grid.Column="1" Grid.Row="0" Value="{TemplateBinding VerticalOffset}" Maximum="{TemplateBinding ScrollableHeight}" ViewportSize="{TemplateBinding ViewportHeight}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />
                            <ScrollBar Name="PART_HorizontalScrollBar" Grid.Column="0" Grid.Row="1" Orientation="Horizontal" Value="{TemplateBinding HorizontalOffset}" Maximum="{TemplateBinding ScrollableWidth}" ViewportSize="{TemplateBinding ViewportWidth}" Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" />
                            <Rectangle Grid.Column="1" Grid.Row="1" Fill="#121214" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#232329" />
            <Setter Property="Foreground" Value="#E2E2E9" />
            <Setter Property="BorderBrush" Value="#3F3F46" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="CaretBrush" Value="#4D7CFE" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#52525B" />
                            </Trigger>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4D7CFE" />
                                <Setter Property="Background" Value="#16161A" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#232329" />
            <Setter Property="Foreground" Value="#E2E2E9" />
            <Setter Property="BorderBrush" Value="#3F3F46" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}" Foreground="{TemplateBinding Foreground}" BorderThickness="{TemplateBinding BorderThickness}" Grid.Column="2" Focusable="false" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="24" />
                                                </Grid.ColumnDefinitions>
                                                <Path Name="Arrow" Grid.Column="1" Fill="#9CA3AF" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="BorderBrush" Value="#52525B" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="10,3,30,3" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Name="DropDownBorder" Background="#16161A" BorderThickness="1" BorderBrush="#232329" CornerRadius="6" Margin="0,2,0,0" />
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#E2E2E9" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Name="Bd" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#232329" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#6B7280" />
            <Setter Property="BorderThickness" Value="0,0,0,2" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" Cursor="Hand">
                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="#4D7CFE" />
                                <Setter Property="BorderBrush" Value="#4D7CFE" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#E2E2E9" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="#16161A" />
            <Setter Property="Foreground" Value="#E2E2E9" />
            <Setter Property="BorderBrush" Value="#232329" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="RowBackground" Value="#16161A" />
            <Setter Property="AlternatingRowBackground" Value="#1A1A1E" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="#232329" />
            <Setter Property="VerticalGridLinesBrush" Value="#232329" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="AutoGenerateColumns" Value="True" />
            <Setter Property="CanUserDeleteRows" Value="True" />
            <Setter Property="CanUserReorderColumns" Value="False" />
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#16161A" />
            <Setter Property="Foreground" Value="#E2E2E9" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#232329" />
                    <Setter Property="Foreground" Value="#FFFFFF" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="Background" Value="#1C1C22" />
            <Setter Property="Foreground" Value="#E2E2E9" />
            <Setter Property="BorderBrush" Value="#232329" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#2D2D35" />
                    <Setter Property="BorderBrush" Value="#4D7CFE" />
                    <Setter Property="Foreground" Value="#FFFFFF" />
                </Trigger>
                <Trigger Property="IsEditing" Value="True">
                    <Setter Property="Background" Value="#121214" />
                    <Setter Property="BorderBrush" Value="#4D7CFE" />
                    <Setter Property="Foreground" Value="#E2E2E9" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1A1A1E" />
            <Setter Property="Foreground" Value="#6B7280" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="BorderBrush" Value="#232329" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
        </Style>

    </UserControl.Resources>

    <Grid Background="#121214">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1A1A1E" Padding="16,6" BorderBrush="#232329" BorderThickness="0,0,0,1">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button Content="← Back to Hub" Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,12,0" Click="BackToMenu_Click" Cursor="Hand" />
                    <Button Content="📄 File ▾" Style="{StaticResource ToolbarButtonStyle}"
                            Click="FileMenuButton_Click" Cursor="Hand">
                        <Button.ContextMenu>
                            <ContextMenu x:Name="FileMenu">
                                <MenuItem Header="Save File" Click="SaveFile_Click" />
                                <Separator Background="#232329" Margin="0,4" />
                                <MenuItem Header="Open Project Folder" Click="OpenFolder_Click" />
                                <MenuItem x:Name="RecentFoldersMenu" Header="Recent Projects">
                                    <MenuItem Header="No recent projects" IsEnabled="False" />
                                </MenuItem>
                            </ContextMenu>
                        </Button.ContextMenu>
                    </Button>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Base URL:" Foreground="#6B7280" VerticalAlignment="Center" Margin="0,0,8,0" FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold" />
                    <TextBox x:Name="BaseUrlInput" Text="https://gitlab.com/pavlikhtp/fairex-cb2-content/-/raw/main" Width="240" VerticalAlignment="Center" Margin="0,0,16,0" Style="{StaticResource ModernTextBoxStyle}" />
                    <TextBlock Text="Server Folder:" Foreground="#6B7280" VerticalAlignment="Center" Margin="0,0,8,0" FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold" />
                    <TextBox x:Name="ServerFolderInput" Text="Specify server folder there" Width="75" VerticalAlignment="Center" Margin="0,0,16,0" Style="{StaticResource ModernTextBoxStyle}" />
                    <TextBlock Text="Mode:" Foreground="#6B7280" VerticalAlignment="Center" Margin="0,0,8,0" FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold" />
                    <ComboBox x:Name="PackingModeComboBox" IsEditable="False" Width="160" VerticalAlignment="Center" Margin="0,0,16,0" Style="{StaticResource ModernComboBoxStyle}" SelectedIndex="0">
                        <ComboBoxItem Content="Mode 1: Scan &amp; Index" />
                        <ComboBoxItem Content="Mode 2: Pack to .cbpak" />
                        <ComboBoxItem Content="Mode 3: Unpack" />
                    </ComboBox>

                    <Button Content="Run Process" Background="#4D7CFE" Foreground="White" Padding="16,8" Style="{StaticResource ToolbarButtonStyle}" Click="RunProcess_Click" Cursor="Hand" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="10" />
                <RowDefinition Height="180" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250" MinWidth="150" MaxWidth="450" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#16161A" CornerRadius="6,6,0,0" BorderBrush="#232329" BorderThickness="1,1,1,0" Padding="10,6">
                        <DockPanel>
                            <TextBlock Text="ADDON FILES" Foreground="#4B5563" FontFamily="Segoe UI" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" DockPanel.Dock="Left" />
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="🔄" ToolTip="Refresh Folder" Style="{StaticResource ActionIconButtonStyle}" Click="RefreshTree_Click" />
                            </StackPanel>
                        </DockPanel>
                    </Border>

                    <Border Grid.Row="1" Background="#16161A" CornerRadius="0,0,6,6" BorderBrush="#232329" BorderThickness="1">
                        <TreeView Name="FileTree" Background="Transparent" BorderThickness="0" Padding="4" SelectedItemChanged="FileTree_SelectedItemChanged">
                            <TreeView.Resources>
                                <HierarchicalDataTemplate DataType="{x:Type models1:FileNode}" ItemsSource="{Binding Value}">
                                    <StackPanel Orientation="Horizontal" Margin="0,1,0,1">
                                        <Grid x:Name="IconGrid" Width="22" Height="12" Margin="0,0,6,0" VerticalAlignment="Center">
                                            <Path x:Name="FolderIcon" Width="15" Height="10.5" Data="M 1,3 C 1,2 2,1 3,1 L 6,1 L 8,3 L 15,3 C 16,3 17,4 17,5 L 17,14 C 17,15 16,16 15,16 L 3,16 C 2,16 1,15 1,14 Z" Fill="#6B7280" Stretch="Fill" Visibility="Collapsed" />
                                            <Border x:Name="IniIcon" Background="#10B981" CornerRadius="2" Width="22" Height="10" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                                                <TextBlock Text="INI" Foreground="#FFFFFF" FontSize="6.5" FontWeight="Black" FontFamily="Segoe UI" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-1,0,0" />
                                            </Border>
                                            <Border x:Name="JsonIcon" Background="#D97706" CornerRadius="2" Width="22" Height="10" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                                                <TextBlock Text="JSON" Foreground="#FFFFFF" FontSize="6.5" FontWeight="Black" FontFamily="Segoe UI" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-1,0,0" />
                                            </Border>
                                            <Border x:Name="DefaultFileIcon" Background="#6B7280" CornerRadius="2" Width="22" Height="10" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                                                <TextBlock Text="FILE" Foreground="#FFFFFF" FontSize="6.5" FontWeight="Black" FontFamily="Segoe UI" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-1,0,0" />
                                            </Border>
                                        </Grid>
                                        <TextBlock Text="{Binding Key}" VerticalAlignment="Center" Foreground="#E2E2E9" />
                                    </StackPanel>
                                    <HierarchicalDataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding IsDirectory}" Value="True">
                                            <Setter TargetName="FolderIcon" Property="Visibility" Value="Visible" />
                                            <Setter TargetName="IconGrid" Property="Width" Value="15" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsDirectory}" Value="False">
                                            <Setter TargetName="DefaultFileIcon" Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Extension}" Value=".ini">
                                            <Setter TargetName="DefaultFileIcon" Property="Visibility" Value="Collapsed" />
                                            <Setter TargetName="IniIcon" Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Extension}" Value=".jsonc">
                                            <Setter TargetName="DefaultFileIcon" Property="Visibility" Value="Collapsed" />
                                            <Setter TargetName="JsonIcon" Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Extension}" Value=".json">
                                            <Setter TargetName="DefaultFileIcon" Property="Visibility" Value="Collapsed" />
                                            <Setter TargetName="JsonIcon" Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </HierarchicalDataTemplate.Triggers>
                                </HierarchicalDataTemplate>
                            </TreeView.Resources>
                        </TreeView>
                    </Border>
                </Grid>

                <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="Transparent" Cursor="SizeWE" />

                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#1A1A1E" BorderBrush="#232329" BorderThickness="1,1,1,0" CornerRadius="6,6,0,0" Padding="14,8">
                        <TextBlock x:Name="CurrentFileNameText" Text="No file selected" Foreground="#E2E2E9" FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold" />
                    </Border>

                    <Border Grid.Row="1" Background="#16161A" CornerRadius="0,0,6,6" BorderBrush="#232329" BorderThickness="1">
                        <Grid>
                            <ScrollViewer x:Name="InspectorScrollViewer" Padding="20" Visibility="Collapsed">
                                <StackPanel x:Name="InspectorPanel">
                                    <TextBlock Text="FILE INSPECTOR" Foreground="#4D7CFE" FontSize="14" FontWeight="Bold" Margin="0,0,0,20" FontFamily="Segoe UI" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" MinHeight="32" />
                                            <RowDefinition Height="Auto" MinHeight="32" />
                                            <RowDefinition Height="Auto" MinHeight="32" />
                                            <RowDefinition Height="Auto" MinHeight="32" />
                                            <RowDefinition Height="Auto" MinHeight="32" />
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Export Path:" Foreground="#6B7280" VerticalAlignment="Center" FontFamily="Segoe UI" FontSize="13" />
                                        <TextBox Grid.Row="0" Grid.Column="1" x:Name="InspectorExportPath" IsReadOnly="True" VerticalAlignment="Center" Padding="5" FontFamily="Consolas" />

                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="MD5 Hash:" Foreground="#6B7280" VerticalAlignment="Center" FontFamily="Segoe UI" FontSize="13" />
                                        <TextBox Grid.Row="1" Grid.Column="1" x:Name="InspectorHash" IsEnabled="False" Style="{StaticResource ModernTextBoxStyle}" VerticalAlignment="Center" FontFamily="Consolas" />
                                        
                                        <TextBlock Grid.Row="2" Grid.Column="0" Text="File Size:" Foreground="#6B7280" VerticalAlignment="Center" FontFamily="Segoe UI" FontSize="13" />
                                        <TextBlock Grid.Row="2" Grid.Column="1" x:Name="InspectorSize" Foreground="#E2E2E9" VerticalAlignment="Center" Padding="5" FontFamily="Segoe UI" />

                                        <TextBlock Grid.Row="3" Grid.Column="0" Text="URL:" Foreground="#6B7280" VerticalAlignment="Center" FontFamily="Segoe UI" FontSize="13" />
                                        <TextBox Grid.Row="3" Grid.Column="1" x:Name="InspectorUrlOverride" Style="{StaticResource ModernTextBoxStyle}" Width="350" HorizontalAlignment="Left" VerticalAlignment="Center" />

                                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Status Validation:" Foreground="#6B7280" VerticalAlignment="Center" FontFamily="Segoe UI" FontSize="13" />
                                        <Border Grid.Row="4" Grid.Column="1" x:Name="StatusBadge" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,0,0,0" MouseDown="StatusBadge_MouseDown" Cursor="Hand">
                                            <TextBlock x:Name="StatusBadgeText" Foreground="White" FontSize="11" FontWeight="Bold" FontFamily="Segoe UI" />
                                        </Border>
                                    </Grid>
                                </StackPanel>
                            </ScrollViewer>

                            <Grid x:Name="EditorPanel" Visibility="Collapsed">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <avalonEdit:TextEditor x:Name="CodeEditor" Grid.Row="0" Background="#121214" Foreground="#E2E2E9" FontFamily="Consolas" FontSize="13" Padding="10" ShowLineNumbers="True" LineNumbersForeground="#4B5563" />
                                <Border Grid.Row="1" Background="#1A1A1E" Padding="10,6" BorderBrush="#232329" BorderThickness="0,1,0,0">
                                    <Button Content="Save Changes" HorizontalAlignment="Right" Padding="12,6" Background="#4D7CFE" Foreground="White" Style="{StaticResource ToolbarButtonStyle}" Click="SaveFile_Click" Cursor="Hand" />
                                </Border>
                            </Grid>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>

            <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" Background="Transparent" Cursor="SizeNS" />

            <Border Grid.Row="2" Background="#0D0D10" CornerRadius="6" BorderBrush="#232329" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="32" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#131317" BorderBrush="#232329" BorderThickness="0,0,0,1"
                            CornerRadius="5,5,0,0" Padding="12,0">
                        <DockPanel>
                            <TextBlock Text="CONSOLE OUTPUT" Foreground="#4B5563" FontFamily="Segoe UI" FontSize="11"
                                       FontWeight="Bold" VerticalAlignment="Center" DockPanel.Dock="Left" />
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Button Content="📋 Copy All" Style="{StaticResource ActionIconButtonStyle}"
                                        Click="CopyConsole_Click" Cursor="Hand" Padding="8,4" Margin="0,0,6,0" />

                                <ToggleButton x:Name="AutoscrollToggle" Content="⬇ Autoscroll" IsChecked="True"
                                              Checked="AutoscrollToggle_Checked" Cursor="Hand" Margin="0,0,6,0">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Background" Value="Transparent" />
                                            <Setter Property="Foreground" Value="#9CA3AF" />
                                            <Setter Property="BorderThickness" Value="0" />
                                            <Setter Property="Padding" Value="8,4" />
                                            <Setter Property="FontSize" Value="11" />
                                            <Setter Property="FontWeight" Value="SemiBold" />
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center"
                                                                              VerticalAlignment="Center" />
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#232329" />
                                                    <Setter Property="Foreground" Value="#F3F4F6" />
                                                </Trigger>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter Property="Background" Value="#1A1A22" />
                                                    <Setter Property="Foreground" Value="#4D7CFE" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                                <Button Content="🗑 Clear" Style="{StaticResource ActionIconButtonStyle}"
                                        Click="ClearConsole_Click" Cursor="Hand" Padding="8,4" />
                            </StackPanel>
                        </DockPanel>
                    </Border>

                    <ScrollViewer Name="ConsoleScrollViewer" Grid.Row="1" VerticalScrollBarVisibility="Auto" CanContentScroll="True">
                        <ItemsControl Name="ConsoleOutput" Padding="12"
                                      VirtualizingStackPanel.IsVirtualizing="True"
                                      VirtualizingPanel.ScrollUnit="Pixel"
                                      VirtualizingPanel.VirtualizationMode="Recycling">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Text, Mode=OneWay}" Foreground="{Binding Color}"
                                               FontFamily="Consolas"
                                               FontSize="13" TextWrapping="Wrap" Margin="0,0,0,2" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <Border Grid.Row="2" Background="#111114" BorderBrush="#232329" BorderThickness="0,1,0,0"
                            Padding="8,6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text=">" Foreground="#4D7CFE" FontFamily="Consolas"
                                       FontSize="13" FontWeight="Bold" VerticalAlignment="Center" Margin="6,0,8,0" />
                            <TextBox Grid.Column="1" x:Name="TerminalInput" Background="Transparent"
                                     Foreground="#D4D4D4" BorderThickness="0"
                                     FontFamily="Consolas" FontSize="13" VerticalAlignment="Center"
                                     KeyDown="TerminalInput_KeyDown"
                                     CaretBrush="#4D7CFE" />
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</utils:LifecycleUserControl>
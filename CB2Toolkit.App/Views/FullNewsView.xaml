<UserControl x:Class="CB2Toolkit.Views.FullNewsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="618" d:DesignWidth="950">
    
    <UserControl.Resources>
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#232329"/>
            <Setter Property="Foreground" Value="#F3F4F6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2D2D35"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#121214">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="40,30,40,0">
            <Button Content="⬅ Back to Hub" 
                    Style="{StaticResource ToolbarButtonStyle}"
                    HorizontalAlignment="Left"
                    Click="Back_Click" 
                    Cursor="Hand"/>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="40,20,40,30">
            <StackPanel MaxWidth="800" HorizontalAlignment="Left">
                <TextBlock x:Name="NewsTitle" Text="Loading Title..." FontSize="26" FontWeight="Black" Foreground="#F3F4F6" TextWrapping="Wrap"/>
                <TextBlock x:Name="NewsDate" Text="Loading Date..." Foreground="#6B7280" FontSize="13" Margin="0,8,0,20"/>
                <TextBlock x:Name="NewsContent" Text="Loading Content..." 
                           Foreground="#9CA3AF" FontSize="14" TextWrapping="Wrap" LineHeight="22"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>